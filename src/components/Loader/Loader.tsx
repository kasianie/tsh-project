import { LoaderIcon } from 'components/icons';
import { StyledLoaderWrapper } from './styles';

export const Loader = () => (
    <StyledLoaderWrapper>
        <LoaderIcon />
    </StyledLoaderWrapper>
);
