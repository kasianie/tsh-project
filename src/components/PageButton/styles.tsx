import { Button, styled } from '@mui/material';

export const StyledPageButton = styled(Button)({
    padding: 0,
    minWidth: 'unset',
    ':hover': {
        background: 'none'
    },
    ':active': {
        background: 'none'
    }
});
